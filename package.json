{
  "name": "alarm-clock",
  "version": "0.0.1",
  "scripts": {
    "start": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "pretty": "prettier --write ./* --end-of-line crlf"
  },
  "devDependencies": {
    "@types/three": "^0.163.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.4",
    "vite": "^2.9.18"
  },
  "dependencies": {
    "three": "^0.163.0"
  }
}
